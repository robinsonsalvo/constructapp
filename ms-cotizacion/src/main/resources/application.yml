spring:
  application:
    name: ms-cotizacion
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

<<<<<<< HEAD
server:
  port: ${SERVER_PORT:8088}

=======
  cloud:
    compatibility-verifier:
      enabled: false

server:
  port: ${SERVER_PORT:8088}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

>>>>>>> 16dc53c (fix de eureka, pruebas unitarias y swagger auth)
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://localhost:3306/db_ms_cotizacion?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false

---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
<<<<<<< HEAD
    open-in-view: false
=======
    open-in-view: false
>>>>>>> 16dc53c (fix de eureka, pruebas unitarias y swagger auth)
